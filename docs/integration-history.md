# 历史集成记录（非当前状态）

# 个人号评论与私信回复试装

账号类型：用户明确确认普通个人号。范围仅自己的作品评论，以及别人发来的私信；不扩展陌生人营销或主动搭讪。

第三方代码分别位于 `douyin-cli`（Yht20927）和 `DMShoot`（Agying3）。两者不是同一个应用，暂未建立统一自动化服务。

## 最新状态：2026-09-15 23:12

用户最新边界：评论线程最多自动回复1次；私信同会话优先回复1次、最多2次，第二次须对方再发消息，之后交本人。不确定内容持久保存到个人回复库handoffs，不在后续批次自动重试。评论相关规则和跨批次去重/交接测试已落实，私信限额为待批次发送器接入的政策与辅助函数。回复模块13项测试通过，没有新增外发。

最终需求与新入口（2026-09-16）：用户明确改为按需自动发送，无须每条确认，并要求按其历史评论/私信回复建库。新入口为 accounts/蛤鸭-抖音/reply-assistant/README.md（相对 AI+自媒体），政策见 policy.json。真实首批导入：12条可参考评论回复，82条历史私信原料中27条可配对的账号回复，24条无可靠上文仅存档；已知系统测试回复各1条排除。按渠道检索和模型拟稿已实测；新评论批次执行器的10项测试通过，底层comment CLI261项测试通过，本轮无额外外发。自动批次未来仅在用户明确请求“回评论”时调用 --send，默认最多5个候选且至少60秒串行间隔，异常投递停止并阻断重试；不承诺免风控。新模块目前没有私信批次执行器。旧的“每条先确认”描述不再适用于此后明确授权的评论批次，早期历史记录保留作追溯。

当前使用方式（用户2026-09-16明确调整）：不建设持续监视或秒回值守；仅在用户明确请求时处理本人作品评论。每次先读取未回复评论并确认本批目标/内容，串行少量发送，逐条检查结果，批次结束即停；不并发、不默认清空历史、不因离线后上线自动补发。私信仅保留已验证工具能力，不默认启动自动回复。缓慢发送只是操作节奏，不是平台认可或不封号保证；平台限制/验证码/发送状态不明确时停止，不改速度绕过。此条取代此前“在线自动回复＋上线补回复”的规划，不代表已关闭用户手动打开的所有程序。

最新验收（2026-09-16 00:25）：用户明确授权回复后，先重新读取 my 确认本人作品，再读取目标父评论确认文案及回复数为0。仅发布“谢谢认可，能讲清楚就好。”，回复 cid=7685797023262753577；读回时父评论 7685586278914163519 的 children 含该回复且作者 uid 为本人。local/comment-review.json 已标记 published_and_readback_verified，禁止重复发送。私信及公开评论均已单条真实验证；没有开启批量或持续自动回复。

本次发送前修复：旧 userscript 在 *.douyin.com 的 iframe 内也注册同一 site，导致 lf-zt.douyin.com 内嵌存储页面抢占连接。脚本新增精确 www 主站 match、@noframes、顶层页面守卫；私有副本已再生成。服务端启用 allowedPollingOrigins，只接受主站注册，拒绝未注册 id 轮询，并退役旧连接等待者。261 项测试通过，桥接服务已重启，现有旧脚本自动重连主页面后完成本次发送；建议后续重新导入新版私有脚本，减少无效 iframe 重试，但当前服务端已阻断这些页面领取命令。

当前状态（2026-09-16）：用户已安装 Tampermonkey、允许用户脚本、导入私有脚本并登录抖音网页。桥接有 1 个活动浏览器连接。douyin-cli `my --count 5` 成功读取本人作品；对 7685472574188424482 读取到两条顶层评论。待批准回复保存在 douyin-cli/local/comment-review.json：针对“讲得很清楚”，拟回复“谢谢认可，能讲清楚就好。”尚未发送。私信测试已由收件方确认收到，禁止再发同一私信测试。自动回复继续关闭。下一步仅在用户批准后重新验证作品归属、目标评论及未回复状态，再发送这一条评论并验证回显。

### 历史记录（以下不是当前状态）

评论链路接入：桥接服务仍健康，连接数为 0；私有 local/douyin.user.js 与 config.json 中令牌一致，服务绑定 127.0.0.1。再次尝试 Chrome 技能仍在初始化阶段失败（node:process 导入受限），未绕过技能接管用户 Chrome。当前需用户手动安装 Tampermonkey，导入该私有脚本，打开并登录抖音网页，再检查 bridge 状态和运行 my/get 只读命令。此脚本含本地桥接令牌，不能分享或上传。评论未读取、未发送。

发送复核：本机监听数据库已出现且仅出现 1 条相同文案的 is_self=1 回显，和接口成功确认一致。真实私信收取→AI草稿→批准后发送→回显这一条测试闭环通过；并未启用无人值守或批量自动回复。

最新真实重登与发送：修复版扫码后 ticket/ts_sign/client_cert/private_key 四项均齐全，SDK has_send_credentials=True。基于此前用户对同一测试账号、同一文案的授权，执行一次凭据修复后的受控重试；原失败记录保留，第二次记录在 approved_test_credential_retry(source_id=1)。创建会话与发送接口返回明确 OK、无 error_desc，状态 api_acknowledged，文案“收到，这是一条自动回复测试。”。不得再次执行该测试发送。尚需收件方确认实际收到；自动回复继续关闭，评论链路仍未完成浏览器接入。

签名采集修复已实现、待真实重登验收：新增 douyin_credentials.py，兼容直接 JSON、data 对象、data 字符串及多层编码；每次仅从同源快照选完整材料，不把不同来源的证书与私钥拼接。登录流程先检查 www.douyin.com，必要时打开消息入口等待网站 SDK 写入，再检查创作者中心，最终回主站复查；移除用 Cookie 假填签名对象的降级逻辑。Auth 统一规范化输入。GUI 与适配器明确显示签名不完整时仅收取，不再建议清理数据。82 项测试通过，2 条上游信号断开警告；旧登录保留、发送仍未验证。当前窗口仍运行旧代码，需要用户关闭窗口（不要点“清理”）后重开安全启动器，再扫码采集。不能因测试通过宣称实际签名采集或发送成功。发送尝试账本仍保留，禁止无条件重发。

最新发送验收：用户批准向测试会话回“收到，这是一条自动回复测试。”。单次尝试失败、无发送回显；已持久化 approved_test_sends(source_id=1) 防重复，不得直接重跑发送脚本。核查保存的 auth 只有 ticket，private_key/ts_sign/client_cert 缺失；用相同 auth 离线构造 create_conversation 请求复现 AttributeError，失败发生在调用消息发送接口之前。接收已验证，发送尚未通过。新增 has_send_credentials 完整性闸门和测试，移除登录采集器的签名值日志。需要修复登录签名采集并重新验证完整性，再安排重新登录；不得把旧 has_ticket=True 当成能发送。自动回复仍关闭。

最新收取验收：用户手动启动监听后重新发送测试私信，实际数据库新增 1 个会话、1 条入站“测试自动回复”，当前未发现重复条目。根据此条实际入站内容调用已配置 AIBackend，生成草稿“收到，这是一条自动回复测试。”；未调用发送接口，auto_reply=False。仅证明这一条收取和草稿生成通过，持久去重及发送回显尚待验收；评论链路仍未接入浏览器。

后续真实校验：用户已完成扫码，数据库存在登录信息；真实 get_my_uid 校验成功，取得私信凭据，单次 WebSocket 握手成功，无错误。短时监听未收到新消息，尚未验证消息到达或发送。测试连接已关闭；GUI 的 douyin_enabled=False、auto_reply=False。下一步用户在“登录→抖音→启动”开启只读监听，再用其他账号发一条测试私信。评论桥接仍为 0 个浏览器连接。补齐漏声明的 protobuf（5.x）和 protobuf3-to-dict；真实 SDK 导入已可用。曾临时装错同名旧 protobuf-to-dict，已卸载并按 SDK 清单替换，未删除用户数据。真实账号验证脚本 `DMShoot/tools/verify_douyin_login.py --listen` 不发送聊天消息、不输出 Cookie 或票据。修复 get_my_uid/get_device_id 的 TLS 校验和超时，并移除私信发送包装器的票据日志。

- 安装已完成：使用 `requirements-local.txt` 中的 PySide6-Essentials；覆盖实际导入的 Qt 模块，不必再下载未使用的 Addons。pip check 通过。
- DMShoot 69 项测试全部通过，有 3 条上游清理/废弃接口警告。完整测试最初发现一处登录按钮测试未使用临时数据库，已补充 temp_db fixture，未削弱断言。
- 实际 AIBackend 鉴权及短文本调用成功；缺少工具名称时正确回复“你说的是哪个工具？发个名字我看看。”这仅是样本通过，不是全面质量验收。
- 修复 GUI 忽略 base_url 和 /v1 重复拼接，所有四处 init_ai 调用都显式传递配置地址；不会静默回退第三方默认接口。
- DMShoot 窗口已启动（进程 5888），本机数据库核验 auto_reply=False、模型配置正确、尚无抖音登录信息。
- 独立扫码窗口使用系统 Edge，不读取已有浏览器档案或发布 Cookie。用户在 DMShoot 点击“登录”→抖音“扫码提取”后亲自扫码。
- 评论桥接运行在 127.0.0.1:19422，健康检查通过，当前浏览器连接数为 0。默认禁用未鉴权 WebSocket，只使用令牌验证的 HTTP 轮询。
- Chrome 技能运行时初始化报错，Computer Use 运行时不可用；因此不能替用户安装扩展或点击扫码按钮，不能声称已接入真实账号。
- 待用户扫码与安装 Tampermonkey 私有脚本后，再验证本人账号、本人作品评论与入站私信；尚未发送真实消息，尚未启动批量值守。

以下为此前阶段记录；安装中等旧状态以上述最新状态为准。

## 当前安全边界

- 尚未登录、未连接真实账号、未发送消息、未设置开机启动。
- 评论桥接仅监听 127.0.0.1:19422；本地 token 已随机生成，私有油猴安装副本在 `douyin-cli/local/douyin.user.js`，不要上传或分享。
- DMShoot 默认自动回复关闭；安全启动器每次强制关闭自动回复和其他平台。
- 不复用发布工具或账号分析的 Cookie。API Key 只在本机应用设置填写，不贴到聊天。
- 试运行遇到验证码、限流或登录异常立即停，不绕过验证。

## 已验证

- douyin-cli：修复 Windows 换行导致提示词解析失败；better-sqlite3 升至兼容本机 Node 24 的 12.x。
- douyin-cli：22 个测试文件、258 个单元测试全部通过；生产依赖 npm audit 为 0。开发依赖仍有上游漏洞告警。
- 单元测试不证明抖音真实接口可用，必须扫码后验证。
- 评论桥接实测 `/api/health` 返回 200，测试进程已关闭。
- DMShoot：关闭自动回复的配置断言与修改文件语法检查通过；恢复 douyin_sdk.py / douyin_im_sync.py 的 TLS 证书校验。
- DMShoot 私信签名桥仅引用 jsrsasign；移除该副本 SDK 中未被私信链路使用的 canvas/jsdom/sdenv/vm 依赖（可按上游 package.json 恢复），npm audit 为 0。未宣称完整 Spider 其他功能仍可运行。
- 2026-09-15 22:42：DMShoot Python 依赖尚未安装完成。PyPI 下载过慢，已停止本次 PyPI 安装并切换阿里云镜像；当前 pip 安装仍在后台运行。未运行 DMShoot 测试套件或启动 GUI，启动脚本尚未完成运行验收。
- 后续先检查正在运行的 pip 安装，不重复启动安装进程；必要时可继续执行 `.venv/Scripts/python.exe -m pip install -r requirements.txt -r requirements-test.txt --index-url https://mirrors.aliyun.com/pypi/simple/`。

## 下一步

### 2026-09-15 本地 AI 接口联调

- 用户提供的本机接口完成鉴权和模型列表读取。密钥仅在被 Git 忽略的本机配置中，不写入本文件。
- gpt-5.4-mini 请求失败（400 / 503，上游账号不可用）；gpt-5.6-luna 短文本生成成功，单次回包报告 379 tokens。金额未知。
- douyin-cli 实际 LLMClient 发出虚拟评论请求并解析 JSON 成功；独立固定字符串校验精确匹配。
- 回复质量尚未过关：未说明具体工具时模型仍断言“适合新手、操作简单”，属于无依据回答。连通性通过不代表可以无人值守，继续关闭自动发送。
- 评论端配置已写入，DMShoot 安全启动器会读取私有本机配置并写入其数据库；DMShoot Python 安装仍在后台，尚未完成该应用实际调用验收。
- 后续更换密钥时同步两个工具；不要把配置文件上传仓库。

1. 完成 DMShoot Python 依赖与离线测试，检查 SDK 依赖告警。
2. 用 Start-DouyinReplies.ps1 -Mode DM 启动私信界面，用户本人扫码，保持自动回复关闭。
3. 用 -Mode Comments 启动评论桥接；Chrome 安装 Tampermonkey，导入私有油猴副本，再登录抖音网页。
4. 读取自己的作品及少量评论；用另一账号发送测试私信，确认收取和去重。
5. 用户选择模型和提供本地 Key，确认回复口吻。先预览回复，再单条真实发送验收。
6. 两条链路都实测通过后再补只处理本人作品、消息去重、限量、失败停止和人工接管的统一自动回复流程。

本试装不代表账号风控可消除，也不代表已经完成自动值守。
