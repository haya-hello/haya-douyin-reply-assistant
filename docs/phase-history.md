# 回复助手阶段历史（非当前入口）

## 2026-09-15 个人号评论与私信回复试装

最新次数与交接规则：用户要求评论一般只回1次、私信1–2次，拿不准交本人。评论执行器已跨批次限制同一父评论只发一次，手动回过跳过；不确定项持久进入handoffs，后续批次不自动放行。私信政策为同会话最多2次、第2次须有新入站、次数不因重启清零；私信批次尚未接入，仅政策和计数辅助函数已就绪。13项回复工具测试通过。本轮只改规则，没有发送消息。

当前正式入口（2026-09-16）：`AI+自媒体/accounts/蛤鸭-抖音/reply-assistant/README.md`。用户最新要求是“明确叫用时按回复库自动逐条发送”，不逐条确认、不持续值守；只有敏感/缺资料/异常项保留。默认每次最多5个候选、发送间隔至少60秒（非免封保证）。首批已真实采集最近5条作品中的12条账号历史评论回复，以及历史私信82条中的27条可配对回复；24条私信缺上文不学习、已知系统测试回复排除。数据库 `data/reply-memory.sqlite3` 为私有，已排除Git。评论/私信检索与基于真实样本拟稿已实测；评论批次 `reply_memory.py reply-comments --send` 已实现且10项模块测试通过，但本轮没有实际批量发送。下一次用户明确要求“回评论”时按该README执行，不再沿用逐条批准流程；读取历史/建库本身不是发送指令。私信批次发送尚未在新模块实现，不能冒充全部自动化完成。

当前需求已改为按需评论协助（2026-09-16）：用户不要持续监视/秒回，只在明确叫用时处理本人视频评论，少量串行、逐条验收、批次结束即停；不自动补发全部历史，不启用私信自动值守。此前持续自动回复规划作废。低频不保证免于平台风控，遇到验证码/限制即停。两条收发链路的单条验证结果仍保留。

最新验收（2026-09-16 00:25）：用户授权后，已在本人作品“大学迷茫，最怕停下和照抄别人”下针对“讲得很清楚”发布“谢谢认可，能讲清楚就好。”，发送返回 cid=7685797023262753577；再次读取评论确认该回复位于正确父评论下，发布者为本人账号。私信、评论两条链路均完成单条真实收发验证，但持续自动回复与无人值守尚未开启。不得重复发布这条测试回复。评论服务修复了内嵌页面抢占连接的问题，261 项测试通过；新版私有脚本已生成，服务端过滤已生效。下一步若用户需要自动值守，应先明确回复范围、频率、知识来源和人工接管条件。

当前状态（2026-09-16）：私信真实收取、生成、批准发送与回显均通过，用户已确认收到；不再重发私信测试，自动回复仍关闭。评论桥接已连上用户 Chrome 的抖音页面，`my --count 5` 成功读取本人作品，`get` 成功读取上一条作品的两条评论；对“讲得很清楚”生成并整理了一条待批准回复，保存在 `AI+自媒体/external/douyin-cli/local/comment-review.json`，尚未发送。下一步经用户确认该文案后，重新核对作品归属和评论目标，仅发一条，再验证回显。双通道无人值守尚未配置。

### 历史阶段记录（以下不代表当前状态）

最新：修复版重新扫码已取得全部发送签名材料。对用户此前批准的同一测试账号/同一文案只进行一次受控重试，抖音发送接口返回成功（api_acknowledged）；重试账本已锁定，禁止重发。待收件方确认，自动回复仍关闭。评论端仍待浏览器桥接，不可宣称双链路自动化完成。

签名修复进展：主站优先采集、嵌套 JSON 解析、同源材料选择及明确“仅收取”状态已实现，82 项测试通过。旧登录与消息保留，自动回复关闭；运行中的 DMShoot 需先由用户关闭窗口（非清理数据），再重开安全启动器并扫码，真实签名及发送能力尚待验证。不要重新执行旧失败发送尝试。

最新阻点：用户批准的单条测试回复未成功发送。保存登录只有 ticket，缺少发送签名私钥/ts_sign/client_cert；离线构造会话请求即失败，发送链路未跑通。接收及生成草稿已通过，自动发送关闭。需修复登录签名采集、重新验证；approved_test_sends 已记录尝试，禁止无条件重发。完整进展见 external/DOUYIN-REPLIES.md。

最新进展：用户启动监听并重发测试后，私信已真实入库（1 会话、1 入站消息），本地 AI 成功生成对应测试回复草稿，未发送、自动回复关闭。下一步为测试账号单条发送与回显验收；评论链路仍需浏览器桥接。详情在 external/DOUYIN-REPLIES.md。

后续：用户已扫码保存账号；真实登录校验与私信 WebSocket 握手均通过，短时收取为 0，未发送消息。测试连接已关闭，GUI 仍未启动监听、自动回复关闭。接下来需用户在 DMShoot“登录→抖音→启动”后由另一账号发测试私信；评论端仍待浏览器脚本连接。已补齐真实 SDK 所需 protobuf 5.x/protobuf3-to-dict，勿将之前“尚无抖音登录信息”当作当前状态。

23:12 最新：DMShoot 安装完成，69 项测试通过，实际 AIBackend 本地接口联调通过；窗口已启动，用户需在“登录→抖音→扫码提取”亲自扫码。评论桥接也已运行，但浏览器连接数为 0，需安装私有油猴脚本。Chrome 与桌面控制运行时均不可用，当前不能代点登录。真实账号未连接、消息未发送、自动回复关闭。详细运行与验收状态见 `AI+自媒体/external/DOUYIN-REPLIES.md`；旧的“依赖下载仍在后台”状态已失效。

本地 AI 接口联调：gpt-5.6-luna 已通过短文本、评论客户端 JSON 解析及固定字符串测试；gpt-5.4-mini 上游不可用。密钥仅保存在私有配置中。回复内容仍有无依据肯定判断，尚未通过口吻与事实边界验收，自动发送继续关闭；DMShoot 依赖下载仍在后台，真实账号尚未连接。

用户确认普通个人号，并选择 douyin-cli 评论 + DMShoot 私信组合。第三方源码在 `AI+自媒体/external/douyin-cli` 与 `AI+自媒体/external/DMShoot`；当前交接见 `AI+自媒体/external/DOUYIN-REPLIES.md`。评论工具经 Node 24 SQLite 兼容与 Windows 换行修复后，258 个单元测试通过。当前未登录真实账号、未发送消息、未启用自动值守。DMShoot 默认自动回复关闭，使用 `AI+自媒体/external/Start-DouyinReplies.ps1` 安全启动。继续时先核对依赖安装和私信测试结果，再安排用户扫码、模型配置及单条发送验收；不得复用发布工具 Cookie。
