# 按需回复工具执行约束

- 正式入口是START_HERE.md，机器规则为policy.json。用户说“回评论”自动路由到此处，不要求记路径或专用名词。
- 时间窗口是源消息最近72小时，不是视频发布时间或导入时间；历史样本可更早。
- “看看”“更新回复库”“体检”“固化”“恢复验收”不是发送许可。只有当次明确要求回复，才运行统一入口reply -ConfirmSend。
- 已授权的评论批次逐条自动发送，无须每条确认；缺信息或敏感内容保留给本人。
- 不用原始post、suggest --auto、测试发送脚本绕过V1限制。禁止清空send_attempts、handoffs、runtime_meta或运行锁来重试。
- 顺序：status → 必要时start → doctor -Online → 用户要求的动作 → 报告已发/跳过/待人工。
- 发送前核对账号、作品归属、原评论时间及本人已有回复。图片、时间缺失、未读全的回复线程保守跳过。
- 同一线程只回一次；次数和至少60秒间隔跨批次保存。未知结果阻断后续外发。
- 私信批次使用统一dm-preview/dm-reply入口及doctor-dm，不以旧DMShoot自动回复开关替代。同会话最多2次，第二次必须新入站；快照失败、接收者不明确或未取得发送回显时停止，不重试。
- 评论和私信共用reply-run.lock与跨批次发送间隔，不能并发启动。读取或更新历史不是发送授权。
- 冻结后不直接更新上游或npm/pip。变更走新版本与回归，禁止覆盖v1.0.0。
- 数据和凭据不进入公开仓库或第二大脑。备份只在副本移除config记录，不改原库。
- 不结束用户Chrome、DMShoot或其他应用做例行验收。维护命令只管理有实例标识的评论桥接。
- 没有浏览器连接时停止并报告，不接管其他账号或自动寻找Cookie替代。
